#include <stdio.h>

/**
 * main - print alphabet in lowercase.
 *
 * Return:0 on success
 */
int main(void)
{
	int n;

	print_alphabet (n = 'a'; n <= 'z'; n++);
	{
		_putchar(n);
	}
	_putchar('\n');

	return (0);
}
